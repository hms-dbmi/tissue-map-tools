# Global debug flags.
# The library is under active construction, so PRINT_DEBUG is kept on by default
# to help catch issues early. Set to False to suppress verbose output.
PRINT_DEBUG = True
VISUAL_DEBUG = False
